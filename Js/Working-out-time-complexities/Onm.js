function printPairs(arr1, arr2) {
  for (let i = 0; i < arr1.length; i++) {
    // O(n)
    for (let j = 0; j < array.length; j++) {
      // O(m)

      console.log(arr1[j], arr2[j]);
    }
  }

  // time complexity => O(n*m)
  // Vi la 2 array rieng biet
}
